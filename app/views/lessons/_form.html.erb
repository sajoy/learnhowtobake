<%= form_for [@section, @lesson] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f. label :content %>
  <%= f.text_field :content %>
  <%= f. label :number %>
  <%= f.number_field :number %>
  <!-- f.collection_select(:lesson, :section_id, Section.all, :id, :name, prompt: true) -->
  <%= f.submit %>
<% end %>
